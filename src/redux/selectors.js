export const getContacts = state => state.contacts.contacts;

export const getFilterDataValue = state => state.filter;

export const selectError = state => state.contacts.error;

export const selectIsLoading = state => state.contacts.isLoadding;